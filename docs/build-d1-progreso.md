# Build D1 — Progreso (fuente de verdad de la ejecución)

> **Protocolo:** cada sesión de build (Opus 4.8, máximo esfuerzo) lee este archivo PRIMERO, ejecuta ÚNICAMENTE la siguiente SESIÓN pendiente (S1/S2/S3, cada una = 2 bloques), y lo actualiza al cerrar. Un checkbox solo se marca con su verificación en verde. Si algo bloquea, va a "Bloqueado" — nunca se improvisa alrededor de un gate.
>
> Plan maestro: `e:\Bobeda Kevin\proyectos\botica-huayruro-sistema-automatizacion-plan-d1.md` (incluida adenda §19).
> Plan de expansión: `e:\Bobeda Kevin\proyectos\botica-huayruro-sistema-automatizacion-plan-expansion.md`.
> Rama: `d1-rebuild` (commit por entregable: "E<N>: <qué>").

**Estado global:** 🟢 **P0 PILOTO EN PRODUCCIÓN** — E12 desplegado y verificado EN VIVO (GATE 4 ✅). URL: `https://huayruro.k-alexander-m-g.workers.dev` (noindex, seed sintético). Kevin autorizó el deploy piloto (2026-07-06). Migraciones + seed en D1 remota, `wrangler deploy`, y verificación en navegador real a 1366×768 y 390×844. **GATE 4 atrapó y se corrigieron dos defectos solo-prod:** (1) login 500 porque Cloudflare Workers rechaza PBKDF2 >100k iteraciones (bajado a 100k = tope de plataforma, hashes de seed regenerados); (2) el HTML de la SPA se servía sin noindex porque los assets no pasan por el Worker (meta robots + `_headers`). Verificado en prod: login OK, catálogo, venta atómica S/1.80 end-to-end, idempotencia, anulación, noindex. **Gate verde: typecheck · API 39/39 · build SPA · smoke navegador local 17/17 y prod OK, cero errores de consola.** Commits `5796bc5` (fix) · `654a8f4` (retiro apps/admin) · **rama `d1-rebuild` pusheada a origin** (2026-07-06, Kevin autorizó). · **E12.5 COMPLETO** (apps/admin retirado, push hecho, frontmatter regla 7, URL entregada). · **Único pendiente real: E12.4 — catálogo/usuarios reales (T-K4/T-K5); el piloto corre con seed sintético.** · **Importador T-K4 COMMITEADO `dcdd78a` y DESPLEGADO A PROD con OK de Kevin (2026-07-06 noche, Version `ac6be920`; smoke: /api/salud 200 · ruta importador 401 sin auth · noindex meta+header intactos).** · **PIVOTE 06-jul (Kevin):** P1 (clientes) y P4a (venta cruzada) POSTERGADOS; lo siguiente son los frentes nuevos **B7–B11** (catálogo maestro → comparador proveedores → bot Telegram → audio A10 → EBR/cámaras) según el plan `e:\Bobeda Kevin\proyectos\botica-huayruro-sistema-automatizacion-plan-frentes-nuevos.md`. **Siguiente sesión: S4 (B7 + B8.1).** · **S4 EJECUTADA Y DESPLEGADA 2026-07-07 (OK de Kevin): GATE B7 VERDE** — maestro 15,181 + FTS + alta asistida + proveedores/listas (commits `bd0d092`→`498f43d`); migración 0002 + maestro aplicados a D1 REMOTA (COUNT/FTS/GTIN verificados) y **deploy prod Version `c6ea9824`** (smoke: salud 200 · maestro/proveedores/importador 401 sin auth · noindex header+meta · e2e login→buscar "ibúprofeno" 20 resultados→por-gtin OK). **Siguiente sesión de build: S5 (B8.2 matching + B8.3 motor + B8.4 UI).** · **S5 EJECUTADA Y DESPLEGADA 2026-07-07 (OK de Kevin): GATE B8 VERDE** (matching GTIN→alias→nombre→fuzzy con aprendizaje de alias + motor de pedido con consolidación exacta ≤2 proveedores + UI de revisión y página Pedido; shared 170/170 · API 79/79 · PWA 5/5 · smoke navegador 0 errores). Commits `196a19b`/`bab0828`/`1955582` **pusheados a origin** y **DEPLOY PROD Version `dc2743ae`** (S5 no trae migraciones nuevas — 0002 ya estaba en la D1 remota desde S4; solo código). Smoke prod: salud 200 · pedidos/matchear/comparar 401 sin auth · noindex X-Robots-Tag intacto. · **S6 EJECUTADA 2026-07-07: GATE B9 VERDE** (bot de Telegram de inventario + bandeja de aprobación web; shared 160/160 · API 92/92 · PWA 5/5 · typecheck · build). Commits `6b17174`→`a0b4793` (+docs `f8ee4d0`) **pusheados a origin** y **DESPLEGADOS a prod con OK de Kevin — Version `4135efa1`** (bucket R2 `huayruro-media` creado + migración 0003 aplicada a la D1 remota + `wrangler deploy`; bindings en vivo: DB · MEDIA · AI · ASSETS). Smoke prod: salud 200 + noindex `X-Robots-Tag` · webhook sin/mal secret → 401 (fail-closed) · bandeja sin auth → 401. **BOT ACTIVADO (T-K12 recibido):** secrets `TELEGRAM_BOT_TOKEN` + `TELEGRAM_WEBHOOK_SECRET` puestos y webhook registrado en Telegram → **`@Asis_Inventario_bot`** (getWebhookInfo: url OK, 0 pendientes, sin last_error, allowed_updates message/callback_query). **Para operar:** un admin genera código en `#/recepciones-pendientes` → la persona escribe `vincular NNNNNN` al bot → `nuevo`. **Pendiente menor:** aceptar UNA vez la licencia Meta del modelo de visión (hasta entonces las fotos caen a texto — el flujo por texto YA funciona end-to-end). **S7 EJECUTADA 2026-07-08: GATE PARCIAL B10.1 VERDE** (typecheck + tests parciales; el GATE B10 completo es S8). Grabadora PWA del A10 (`#/grabadora`, modo dispositivo, MediaRecorder opus 30 s con grabador rotativo + descarte de silencio RMS + cola offline Dexie FIFO 200 MB) → `POST /api/audio` (auth de dispositivo, idempotente por client_uuid) → R2 `audio/{tenant}/{fecha}/{uuid}.webm` + `audio_grabacion` 'subido' → transcripción Whisper turbo (`@cf/openai/whisper-large-v3-turbo`, base64+es, verificado contra docs CF) en `ctx.waitUntil` → 'transcrito'; **Cron `*/5` barredora** de los 'subido' que el waitUntil no alcanzó (`worker.ts`={fetch,scheduled}, `index.ts` sigue exportando el app para los tests). CRUD `/dispositivos` (token una vez + kill-switch). Migración `0004_audio` (audio_grabacion §5.4 + audio_senal §4.3 con **veto D-N5 sin operador_id**) aplicada local. API 107/107 (15 audio) · PWA 19/19 (14 grabadora) · build SPA. Commits `a71943b`+`8d687b5`+`4ed83a6`. **DESPLEGADO a prod con OK de Kevin — Version `656554e7`** (0004 remota + `wrangler deploy` con cron `*/5` registrado + lifecycle R2 `audio-7d` solo prefijo `audio/` + push origin; smoke prod verde: salud 200/noindex, `/api/audio` y `/api/dispositivos` sin token 401). Falta física: **T-K2** (pegar token de dispositivo en el A10 + validar mic/wake lock). **S8 EJECUTADA 2026-07-09: GATE B10 VERDE** (B10.2 extracción de señales + bandeja + veto). `lib/senales.ts` extractor `@cf/meta/llama-3.1-8b-instruct` (id+messages verificados) → `procesarSenales` matchea faltantes por FTS del tenant y cotea venta_posible ±10 min contra las ventas POS (autocierra), todo idempotente; `audioSenalRepo` bandeja + confirmar (faltante → **quiebre REAL sin operador**, D-N5) + descartar; `procesarAudio` en el waitUntil (<2 min) + `barrerSenales` en el Cron; Mostrador con **badge 🎙️** + bandeja. **TEST DEL VETO D-N5** (audio_senal sin operador, ninguna query cruza audio↔personal, solo repos/audio.ts la lee). API **125/125** · PWA 19/19 · typecheck · build SPA. Commits `0f954b5`→`24320b5`. **Sin migración nueva** (0004 ya creó audio_senal en S7). **Fix crítico B10.2.5: `env.AI.run` detachado perdía `this` → transcripción (S7) y OCR (S6) estaban ROTAS en prod (gate con fakes nunca lo vio); modelo elegido `@cf/meta/llama-3.2-3b-instruct` (3.1-8b base deprecado).** **DESPLEGADO A PROD (OK de Kevin) — Version `7a7de524`** (arregla también S6/S7) + push origin; smoke verde. **T-K2 VERIFICADO en vivo** (A10 sube→transcribe→señala end-to-end en prod). **B10.2.6 desplegado (Version `d6200d06`):** sesgo de transcripción (`initial_prompt` con nombres del catálogo + jerga de botica/monedas) + filtro de ruido por confianza (UMBRAL_SENAL=0.4) — motivado por los defectos vistos en la prueba en vivo. **S8.5 = B10.3 EJECUTADA Y DESPLEGADA (2026-07-10, Version `eb04b67f`):** optimización de calidad del audio — correcciones que aprenden (forma oída → producto, sin operador D-N5), reporte de calidad diario (panel `#/audio-calidad`), estado `sin_habla` (flag, ya no ensucia el bucket de errores) y `nombresCatalogoTenant` por corrección>frecuencia>alfabético. Migración `0005` (ADD COLUMN FK-safe tras que el rebuild reventara la FK remota — gotcha durable). shared 160 · API 140 · PWA 19. **Siguiente sesión de build: S9 = B11 (EBR + espejo + clips si T-K3).** Pendiente Kevin: re-test del sesgo con voz fresca (ahora visible en el panel de calidad) + usar el bucle de corregir nombres.

## Mapa de sesiones y qué leer en cada una (ahorro de tokens: NO releer el plan completo)

| Sesión | Bloques | Lecturas (además de este archivo) |
|---|---|---|
| **S1** | B1 + B2 | Plan D1 COMPLETO (única sesión que lo lee entero, incluida adenda §19) + §1 del plan de expansión |
| **S2** | B3 + B4 | Plan D1 SOLO §6, §7, §8, §9, §11 + adenda §19 |
| **S3** | B5 + B6 | Plan D1 SOLO §3, §8, §10, §11, §18 (checklist E12) |
| **S4** | B7 + B8.1 | Plan frentes nuevos §2–§6 (+ shared/csv y shared/catalogo ya en repo) |
| **S5** | B8.2–B8.4 | Plan frentes nuevos §6 + `repos/admin.ts` (faltantes) |
| **S6** | B9 | Plan frentes nuevos §7 + docs Telegram Bot API |
| **S7** | B10.1 | Plan frentes nuevos §8 + Plan D1 §13 y §5.4 |
| **S8** | B10.2 | Plan frentes nuevos §8 (relee §4.3) |
| **S8.5** | B10.3 (optimización de calidad del audio) | Este archivo (sección S8, item B10.3) + `repos/comparador.ts` (patrón `producto_alias` que aprende) |
| **S9** | B11 | Plan frentes nuevos §9 + Plan expansión §3 B1 y §5 D1 |

**Disciplina de tokens (aplica siempre):** tests con reporter silencioso (`--reporter=dot`) y SOLO del paquete tocado mientras desarrollas; la suite completa se corre UNA vez por gate 🚧. Salidas largas de comandos → `| tail -20`. No relee archivos ya en contexto, no imprime archivos completos en el chat, no lanza subagentes salvo necesidad real. La actualización del vault (regla 7) se hace UNA vez por sesión al cierre, no por bloque. Mensaje final: máx. 10 líneas.

---

## B1 — Cimientos (E0 spike + E1 esquema + E2 dinero)

- [x] E0.1 `apps/api` creado (Hono + wrangler.jsonc), `wrangler d1 create huayruro-db` (`da708485-…`), binding DB, assets binding
- [x] E0.2 `@cloudflare/vitest-pool-workers` corriendo (0.8.71 / vitest 2.1.9 / wrangler 4.107)
- [x] E0.3 **SPIKE** (plan §2.2): visibilidad intra-batch ✅ · rollback por CHECK ✅ · ON CONFLICT en batch ✅ · FTS5 remove_diacritics ✅ — **local Y remoto**. Documentado en `docs/adr/011-d1-batch-semantica.md`. **Plan A del §7 confirmado (sin plan B).** Lección: D1 remoto arroja "Network connection lost" transitorio → `withRetry` en repos/base.ts
- [x] E0.4 Keys Supabase muertas eliminadas de `.env.local`
- [x] E1.1 `0001_esquema_p0.sql` = DDL §5.2 **+ Δ1-Δ5** (24 tablas) — aplicada local
- [x] E1.2 Seeds (§5.6) aplicados **local** (10 SKU / 30 precios / 30 inv / 30 lotes / 20 presentaciones / 4 usuarios). Generador: `scripts/gen-seed-d1.mjs`. *(Remoto diferido a después del GATE 3 / E12 con catálogo real T-K4.)*
- [x] E2.1 `packages/shared/src/calculos/dinero.ts` (espec §6.2)
- [x] E2.2 🚧 **GATE 1-2 VERDE:** golden tests §6.3 (60 tests: 10 SKUs × {1,2,3,7} + carritos mixtos + propiedad 1000 + round-trip + regresión total≠round(S×1.18))

## B2 — Seguridad (E3 auth + E4 aislamiento)

- [x] E3.1 Auth: PBKDF2-SHA256 (310k)/WebCrypto, tabla `sesion` (solo SHA-256 del token), cookie+token, sesión deslizante 30d, rate-limit login 5/15min; login/logout/me/password
- [x] E3.2 Middleware `requiereAuth` (actor usuario|dispositivo) + guards de rol + repos con scoping (`env.DB` inyectado SOLO en `repos/contexto.ts`). Regla ESLint escrita (`apps/api/.eslintrc.cjs`); **enforcement real = test #14** (eslint9 sin flat config es deuda preexistente)
- [x] E4.1 Los 14 casos de §4.4 escritos (13 HTTP en `test/aislamiento.test.ts` + #14 en `test/canal-prohibido.test.ts`)
- [x] E4.2 🚧 **GATE 3 (BLOQUEANTE) VERDE:** 17/17 en apps/api (14/14 aislamiento). Datos reales ya habilitados para E12

## B3 — Venta (E5 catálogo + E6 venta atómica)

- [x] E5.1 Endpoints catálogo/precios/sync + FTS5 (test "ibúprofeno"→"Ibuprofeno" verde) + selector de presentación (Δ1). `catalogo.ts`: `porGtin`, `sync` (delta+tombstones), CRUD+FTS en batch, `precioRepo.crearVersion`; rutas barcode/sync/CRUD/POST precios (commit f3f3b70)
- [x] E5.2 PWA conectada — **cutover COMPLETO.** `api.ts`+`sync.ts` (+`sincronizarStock`) + `auth.ts` (login/me/logout + `sesion_cache`, token en memoria) + `useSession` (arranque offline) + `LoginPage` (email+password) + `useCatalogoVenta` (Dexie, selección de presentación Δ1) + `useCarrito` en enteros (§6, sin floats) + `Mostrador` (cobro→cola, guía, reimprimir, quiebre) + `Layout`/`ruta.ts` (router hash por rol). Supabase RETIRADO (borrados `supabase.ts`/`useCatalogo(-Local).ts`/`uuid.ts`; deps `@supabase`/`@powersync`/`@huayruro-db`/`@tanstack` fuera). Commit f84bb0b
- [x] E6.1 `POST /api/ventas` = batch §7.3 TAL CUAL (guardas EXISTS, FEFO cascada, venta_item_lote, retry por CHECK, advertencias, Δ3 evento_caja apertura_venta) (commit 1fcf301)
- [x] E6.2 Anulación §7.6 (guarda por `anulada_motivo` prefijado; sin doble reposición) (commit 1fcf301)
- [x] E6.3 🚧 **GATE VERDE (9 tests):** reintento idéntico · carrera de client_uuid · carrera de lote (CHECK-rollback-retry, sin negativos) · remanente sin lote · doble anulación (409) · golden dinero vía HTTP (S/15.00, S/105.00, regresión total≠round(S×1.18)) · blíster Δ1 factor 10 (commit bc20874)

## B4 — Offline + operación (E7 cola + E8 recepción/caja)

- [x] E7.1 Dexie completo (esquema §9) + flusher FIFO con backoff (1s/5s/30s/5min) + banner de estado + uuidv7 (shared). `db-local.ts`/`cola.ts`/`useEstadoSync.ts`/`BannerSync.tsx` (commit bedfc78)
- [x] E7.2 🚧 **GATE VERDE (ambos lados):** cliente (modo avión→online→3 confirmadas únicas + backoff + rechazada) y D1 (3 client_uuids × 2 envíos → 3 ventas únicas, stock 1 vez) (commits bedfc78, c641f64)
- [x] E8.1 Recepción — **API + UI.** `POST /recepciones` (a827b14) + pantalla `Recepcion.tsx` (form multi-ítem con `SelectorProducto` online). Commit f84bb0b
- [x] E8.2 Inventario — **UI.** `Inventario.tsx`: lista (con nombre) + filtro bajo-mínimo + ajuste por conteo + fijar mínimo (`PATCH /inventario/:productoId/minimo`) + lotes por vencer. Commit f84bb0b
- [x] E8.3 Caja — **UI.** `Caja.tsx`: resumen del día por método + cierre (server calcula diferencia, 409 dup) + historial. Commit f84bb0b

> **Nota de alcance (S3):** el cutover PWA + toda la capa de páginas (B5 incluido) están construidos y con gate verde en typecheck/tests/build. Lo ÚNICO que falta para cerrar P0 es E12 (deploy prod + verificación EN VIVO con navegador), que quedó bloqueado por el clasificador de producción (deploy no solicitado explícitamente por Kevin) y porque GATE 4 requiere navegador. La UI está probada por tipos + unit + integración, pero NO se ha visto en un browser real todavía.

## B5 — Cierre operativo (E9 impresión + E10 faltantes + E11 dashboards/admin)

- [~] E9.1 Impresión ESC/POS + guía 80mm + fallback CSS + reimpresión — **módulo `impresora.ts` LISTO** (WebUSB+ESC/POS bytes inline; guía interna con IGV desglosado; `imprimirPorNavegador` CSS 80mm verificable sin hardware; reimpresión desde `ultimaGuia`). ⏳ **validación WebUSB real + cajón Δ3 = T-K1** (impresora física VES). Commit f84bb0b
- [x] E10.1 Quiebres — `quiebreRepo` + `POST/GET /api/quiebres` (idempotente, offline vía cola) + `QuiebreModal` (botón rápido en Mostrador) + `/api/faltantes` por botica (con quiebres_14d + sugerido). Commits 236e7d8/f84bb0b
- [x] E10.2 Consolidado de faltantes superadmin + CSV — `faltantesRepo.consolidado`/`consolidadoCsv` (fila por producto×botica + TOTAL) + pantalla `Consolidado.tsx` con descarga. Commit 236e7d8/f84bb0b
- [x] E11.1 Dashboards — `dashboardRepo.resumen` (ventas/día, ticket, top SKU, quiebres, stock bajo, margen estimado) + `consolidado` por botica (agregados, nunca detalle) + `Dashboard.tsx`/`Consolidado.tsx`. Commit 236e7d8/f84bb0b
- [x] E11.2 CRUD usuarios/sucursales + formularios de catálogo — `PATCH /usuarios/:id` (activar/reset/rol/sucursal), `POST/PATCH /sucursales`, `POST /catalogo/productos/:id/presentaciones` (Δ1) + pantallas `Usuarios/Sucursales/CatalogoForm` (crear producto + presentaciones + precios por sucursal). Commit 236e7d8/f84bb0b

## B6 — Deploy y piloto (E12)

- [x] E12.1 Deploy prod `workers.dev` + noindex; migraciones + seed remotos. Kevin autorizó (2026-07-06). `migrations apply --remote` (47 cmds), `d1 execute --remote seed` (497 filas), `wrangler deploy`. noindex reforzado con meta robots + `_headers` (el `X-Robots-Tag` del Worker NO llega a los assets). Version prod `856ab18d`.
- [x] E12.2 Smoke: login → buscar (FTS) → carrito → cobrar → anular → caja/dashboard verificado EN VIVO (navegador local 17/17 + prod curl venta atómica S/1.80 + anulación). Guía CSS 80mm capturada en el cobro local. *(Falta browser-verificar: consolidado super_admin y WebUSB físico = T-K1.)*
- [x] E12.3 🚧 **GATE 4 VERDE:** verificación EN VIVO con capturas (mostrador 1366×768 + móvil 390×844), cero errores de consola, contra la URL de prod. Capturas en el scratchpad de la sesión (`shots/` local + `shots-prod/`). La UI ya corrió en navegador real. **Atrapó 2 bugs solo-prod (PBKDF2 100k, noindex) → corregidos y re-verificados.**
- [ ] E12.4 Carga catálogo real VES (**T-K4**) + usuarios reales (**T-K5**) — pendiente; piloto vive con seed sintético.
- [x] E12.5 Retirar `apps/admin` ✅ (commit `654a8f4`, dead code Next+Supabase, D9; lockfile regenerado, typecheck api+pwa verde) · `git push origin d1-rebuild` ✅ (Kevin autorizó 2026-07-06) · frontmatter regla 7 ✅ · URL entregada ✅. Credenciales dev en `apps/api/seeds/0001_seed_p0.sql` (seed sintético, nunca en chat).

---

## Importador de catálogo en lote (habilitador de T-K4) — 2026-07-06 (Opus 4.8)

Herramienta para que Kevin (y luego papá/mamá) carguen su catálogo real desde una hoja CSV, en vez de tipear producto por producto. **Construido completo (regla 11) y con gate verde; NO desplegado a prod** (Kevin pidió "aún sin datos": tener la herramienta lista para cuando tenga la hoja).

- **Contrato de dinero (clave):** la columna `precio_venta` del CSV es el **precio al público CON IGV** (lo que cobra el cliente). Se guarda `precio_sin_igv_dm = round(pvp_dm×100/118)` (`sinIgvDmDesdeVentaPublicaDm` en `packages/shared/src/calculos/dinero.ts`), que **reproduce EXACTO** los 10 precios del seed §5.6 (test dorado). NO invertir el redondeo.
- **Alcance de columnas (Kevin eligió "completo + blíster"):** nombre, código de barras, laboratorio, principio activo, categoría, presentación, requiere_receta, es_cronico, precio_compra, precio_venta, stock_inicial, stock_minimo, numero_lote, fecha_vencimiento, blíster (nombre/factor/precio/código). Plantilla descargable (`PLANTILLA_CSV`).
- **Interfaz (Kevin eligió in-app):** pantalla `apps/pwa/src/pages/admin/CatalogoImportador.tsx` (rol `admin_sucursal`+`super_admin`; ruta `#/importar-catalogo`). Baja plantilla → sube/pega CSV → **previsualiza** (dry-run: qué entra / qué se rechaza y por qué / duplicados / avisos) → confirma. Sección de **purga del catálogo demo** (solo super).
- **Arquitectura:** parser CSV robusto (`packages/shared/src/csv/parse.ts`; detecta `;`/`,`/tab, comillas, BOM) → validador puro (`packages/shared/src/catalogo/importar.ts`) → endpoint autoritativo `POST /api/catalogo/importar` (`?dry_run=1` previsualiza; sin él comete) + `GET /catalogo/importar/plantilla` + `GET /catalogo/demo/conteo` + `POST /catalogo/demo/purgar`. Escritura **atómica por producto** en `db.batch()` (`apps/api/src/repos/importar-catalogo.ts`): 3 desenlaces — `creado` / `agregado_a_sucursal` (producto compartido a nivel tenant, otra sucursal le agrega precio+stock) / `omitido_existente` (idempotente). Purga borra SOLO el seed sintético (marcador `id LIKE '30000000-…' + created_at fijo`), en orden FK, sin tocar ventas.
- **Gate:** typecheck 5/5 · **shared 106/106** · **API 58/58** (importador 19) · build SPA · **smoke navegador real (1366×768) 7/7 con `run_worker_first` local**: login super → previsualiza (3 válidas, 2 con lote, 1 con blíster, 1 sin código; S/1.80/0.90/8.00) → importa 3 → "3 creados". Cero errores de consola propios (los 2 del Mostrador con super sin sucursal son pre-existentes).
- **Revisión adversarial (workflow 5 lentes) → 8 hallazgos, 7 corregidos con tests + 1 documentado:** (F1) precio enorme desbordaba `Number.isSafeInteger` y tumbaba TODO el lote → cota S/100k + rechazo por fila. (F2) coma de miles `1,200` se leía S/1.20 (1000× menos) → se rechaza como ambigua. (F4) comilla suelta se comía el resto del archivo → la comilla solo abre campo al inicio + se detectan comillas sin cerrar (error estructural). (F6) productos SIN código se duplicaban al re-importar → dedup por nombre (intra-archivo + BD) → idempotente. (F5) columna duplicada → se reporta ignorada. (F7) presentación base desactivada → ya no choca UNIQUE. (F3) inyección de fórmulas CSV en la exportación de faltantes → celdas que empiezan con `= + - @` se neutralizan con apóstrofo (fix en `repos/admin.ts consolidadoCsv`). (F9) un super podía escribir en sucursal de OTRO tenant → el endpoint valida que la sucursal sea del tenant del actor. **F8 (documentado, no crítico):** en un corte de red de D1 remoto DESPUÉS de commit, `withRetry` reintenta el batch → choca contra la PK → esa fila se reporta `fallido` aunque SÍ se escribió; no corrompe datos (la PK evita doble), pero el reporte puede mentir en ese caso raro.
- **Limitación conocida (documentada):** para adjuntar un producto compartido existente a una 2ª sucursal solo se copia la presentación base (el blíster se ignora con nota). Modo "actualización de precios" de un catálogo ya cargado = v2.
- **Pendiente humano:** ninguno técnico. Cuando Kevin tenga la hoja real de VES: purgar demo (o re-seed limpio) → importar. **Para usarlo en el piloto vivo hay que desplegar la SPA a prod** (build + stage `public` + `wrangler deploy`), con OK de Kevin.

## Después de P0 — ORDEN VIGENTE (pivote de Kevin 2026-07-06)

**B7** catálogo maestro (SUSALUD GTIN v4, 15,181) → **B8** comparador de listas de proveedores → **B9** bot Telegram de inventario (bandeja aprobación web) → **B10** audio A10 casi-tiempo-real (señales faltante + borrador venta; veto supervisión) → **B11** EBR bandeja de casos + espejo + clips (T-K3). Especificación completa, DDL y gates: `e:\Bobeda Kevin\proyectos\botica-huayruro-sistema-automatizacion-plan-frentes-nuevos.md`. *(P1 clientes, P4a venta cruzada, P3 rostros y P4b quedan postergados; el plan de expansión sigue siendo su spec cuando se retomen.)*

### B7–B11 (frentes nuevos — checkboxes se agregan al abrir cada sesión)

- [x] **S4:** B7 completo + B8.1 — 🚧 **GATE B7 VERDE** (2026-07-07):
  - [x] B7.1 `scripts/gen-catalogo-maestro.mjs` (lector XLSX sin dependencias: zip+XML) → `seeds/0002_catalogo_maestro.sql` (15,181 filas, lotes de 300 — **D1 corta statements >100 KB**, 500 filas dio SQLITE_TOOBIG). `normalizarNombre()` extraída a `shared/catalogo/normalizar.ts` (módulo puro sin imports → el .mjs la importa DIRECTO con type-stripping de Node ≥23: una sola función para importador+maestro+listas). Parsers de valores extraídos a `shared/csv/valores.ts` (reuso en listas). **Nota:** UNIDADENVASE vino 100% poblado → el cruce EAN13-2021 previsto quedó innecesario (documentado en el script). 0 GTIN duplicados en la fuente.
  - [x] B7.2 Migración `0002_abastecimiento.sql` (catalogo_maestro + maestro_fts contenido-externo + proveedor + lista_precios + lista_item + producto_alias + pedido + pedido_item) aplicada **local**; maestro cargado: **COUNT=15,181** · FTS `'ibúprofeno*'`→IBUPROFENO · lookup GTIN exacto — verificados con wrangler d1 local.
  - [x] B7.3 `GET /api/maestro/buscar?q=` (FTS, LIMIT 20) + `GET /api/maestro/por-gtin/:gtin` — repo `maestro.ts` SIN actor (única tabla global, D-N7); rutas con `requiereUsuario`.
  - [x] B7.4 Alta asistida: `BuscadorMaestro` en CatalogoForm (debounce 300 ms, ≥3 letras) precarga nombre/lab/DCI/presentación + **GTIN**; `POST /catalogo/productos` acepta `codigo_barras` → `codigo_barras` de la presentación base EN EL MISMO batch (GTIN duplicado → 422 sin producto huérfano). Aseo/galénicos = mismo form a mano.
  - [x] B8.1 CRUD proveedor (mínimo/flete/días/contacto/activo, UNIQUE nombre→409) + ingesta de listas: `shared/proveedores/lista.ts` (validador puro: precio a céntimos con máx 2 decimales —3+ = rechazo—, bonif "10+1", vencimiento con MM/YYYY→día 01, `parseFactorUnidades` que distingue "CAJA X 100"=100 de "FCO X 120 ML"=null, `esVencCorto` umbral 8 meses T-K13) + `POST /proveedores/:id/listas?dry_run=1` (patrón del importador) → commit en **UN batch atómico** (tope 3,000 filas) + pantalla `#/proveedores` (🚚, admin+super).
  - [x] 🚧 **GATE B7 VERDE:** shared **120/120** (14 nuevos lista) · API **70/70** (12 nuevos: maestro global+tildes+GTIN, alta asistida atómica, CRUD/ingesta, **aislamiento 2 tenants: proveedor/listas/items ajenos → 404, D-N7**) · PWA 5/5 · typecheck 5/5 · build SPA · **smoke navegador real 8/8** (login → buscar "ibúprofeno" → precarga GTIN real → crear → droguería → pegar TSV → previsualizar 3/2/1 → cargar; los 2×400 de consola son los pre-existentes del Mostrador con super sin sucursal).
- [x] **S5:** B8.2 matching + B8.3 motor pedido/consolidación + B8.4 UI — 🚧 **GATE B8 VERDE** (2026-07-07):
  - [x] B8.2 Matching (módulo puro `shared/proveedores/matching.ts`): pipeline GTIN→alias→nombre_exacto→fuzzy, primero-que-pega-gana. Fuzzy = score token-set `0.6·coverage + 0.4·precisión` (coverage = identidad del producto presente en la oferta; precisión penaliza palabras ajenas pero el laboratorio del producto es "gratis") + **guarda de concentración** (400mg≠600mg → acotado a manual). Umbrales ≥0.90 auto · 0.60–0.89 pendiente (top-3) · <0.60 manual. Repo `comparador.ts` carga índices del tenant UNA vez (productos + GTIN + alias del proveedor) y matchea/persiste en batch. **Confirmar APRENDE** `producto_alias` (upsert `ON CONFLICT proveedor_id+texto_norm`) → la misma descripción matchea sola el mes siguiente; descartar suelta el producto (v1: sin alias negativo).
  - [x] B8.3 Motor (módulo puro `shared/proveedores/pedido.ts`): precio efectivo por unidad base con bonif en enteros `round(precio_cent·100·compra/((compra+gratis)·factor))` (D-N1/D-N2); consolidación exacta ≤2 proveedores por enumeración (singles+pares), asigna cada ítem al más barato del combo, flete por proveedor usado, combo inválido si un usado no llega a su mínimo; ranking cobertura DESC/válido/total ASC; **dedup por proveedores realmente usados** (un {A,C} donde C no aporta colapsa a {A}). Repo `pedido.ts`: base = faltantes (`faltantesRepo.miBotica`), comparar (top-3 + venc-corto disponibles), crear (combo → `pedido`+`pedido_item`), **CSV por proveedor** (anti-fórmulas). Venc corto NO se auto-elige (check manual).
  - [x] B8.4 UI: `Proveedores.tsx` botón "🔎 Matchear" por lista → panel de revisión (resumen + sugerencias top-3 + buscar-otro + descartar). `Pedido.tsx` (`#/pedido`, admin/super): faltantes editables + **agregar producto** → comparar → 3 combos con delta S/ + aceptar venc corto → guardar → CSV por proveedor (selector de sucursal para super).
  - [x] 🚧 **GATE B8 VERDE:** shared **170/170** (25 nuevos: matching+pedido golden a mano) · API **79/79** (9 nuevos comparador: GTIN/alias/nombre/fuzzy/**aprendizaje**, golden del motor {A} vs {A,C}+venc corto = S/35 ahorro, CSV, **aislamiento 2 tenants**) · PWA 5/5 · typecheck 5/5 · build SPA · **smoke navegador real (1366×900, 0 errores de consola)**: pegar lista 5 filas → matchear (2 auto / 2 dudosos / 1 sin match) → **confirmar 2 dudosos** (sugerencias correctas) → descartar sin-match → pedido → combo → guardar → CSV con producto+TOTAL. Commits `196a19b` (shared) · `bab0828` (API) · `1955582` (UI). *(Nota smoke: un "sin oferta" de ibuprofeno fue artefacto de la D1 LOCAL sucia — 3 productos ibuprofeno-ish de corridas previas; el golden limpio cubre los 3 productos OK.)*
  - Simplificación v1 anotada (T-K13 valida con combos reales): bonificación modelada como descuento PROPORCIONAL (no escalonado por umbral); descartes de matching se repiten por lista (alias solo para positivos).
- [x] **S6:** B9 bot Telegram + bandeja recepciones — 🚧 **GATE B9 VERDE** (2026-07-07):
  - [x] B9.1 Migración `0003_recepcion_borrador.sql`: `recepcion_borrador` (bandeja), `bot_chat` (máquina de estados + dedup por `update_id`), `bot_vinculacion` (código 6 dígitos), `dispositivo.tipo` ampliado con `telegram_bot` (patrón 12-step SQLite). Bindings `MEDIA` (R2 `huayruro-media`) + `AI` (Workers AI) en wrangler; secretos por `wrangler secret`.
  - [x] B9.2 `shared/telegram/conversacion.ts` (PURO): máquina de estados sin framework (producto→lote→precio→blíster→cantidad→ubicación→resumen) + parsers de troquelado (lote/venc en varios formatos MM-AAAA/MM/AA/AAAA-MM), precio, blíster, cantidad. **Gotcha corregido con test:** no comerse la "L" de un lote real (L9) al limpiar etiquetas.
  - [x] B9.3 Webhook `POST /api/telegram/webhook` (público, montado ANTES de protegidas): valida `X-Telegram-Bot-Api-Secret-Token` (401 fail-closed). `repos/bot.ts`: allowlist DURA por chat_id (chat no vinculado = **silencio total**, ni /start responde), dedup por `update_id`, vinculación por código, OCR-a-estado, crea el borrador; **responde con el método en el cuerpo** (Telegram lo ejecuta → cero llamadas salientes para enviar; el token solo se usa para descargar fotos). `lib/vision.ts`: OCR guarded (`@cf/meta/llama-3.2-11b-vision-instruct`) con fallback a texto ante cualquier fallo. **canal-prohibido 3/3 intacto** (todo el SQL en repos/).
  - [x] B9.4 Bandeja `repos/recepcion-borrador.ts` (tenant-scoped): pendientes enriquecidos por GTIN/maestro, **aprobar = recepción REAL por el MISMO kardex** (reusa `recepcionRepo.registrar` → lotes/FEFO/stock/audit intactos), alta al vuelo del producto (+ precio si el bot lo capturó, vía `sinIgvDmDesdeVentaPublicaDm`), rechazar sin tocar stock, gestión del bot (código/listar/desvincular). Rutas admin+ en `protegidas.ts` + proxy de fotos R2 vía Worker (nunca expone claves R2).
  - [x] B9.5 PWA `#/recepciones-pendientes` (admin+): selector de sucursal (super), sección de vinculación (genera código, lista/desvincula teléfonos), tarjetas por borrador con fotos (proxy autenticado), aprobar/corregir/rechazar y alta precargada con el maestro.
  - [x] 🚧 **GATE B9 VERDE:** shared **160/160** (15 nuevos conversación) · API **92/92** (13 nuevos bot: flujo feliz por texto, foto ilegible→texto, no-vinculado=silencio, secret=401, dedup de `update_id`, aprobación crea recepción real con **stock+lote FEFO verificados**, rechazo no toca stock, alta al vuelo, proxy R2, **aislamiento cross-sucursal + cross-tenant**) · PWA 5/5 · typecheck 3/3 (con tests) · build SPA. Commits `6b17174`/`f543a89`/`1c49c10`/`c790b01`/`a0b4793` (+docs) **pusheados a origin** y **DESPLEGADOS a prod con OK de Kevin — Version `4135efa1`**.
  - **Deploy EJECUTADO (2026-07-07):** (1) `wrangler r2 bucket create huayruro-media` ✅ · (2) `wrangler d1 migrations apply huayruro-db --remote` (0003, 10 comandos) ✅ · (3) build SPA + stage `public` + `wrangler deploy` ✅ (bindings en vivo DB/MEDIA/AI/ASSETS) · (4) secrets `TELEGRAM_BOT_TOKEN` + `TELEGRAM_WEBHOOK_SECRET` (aleatorio de 48) ✅ · (5) `setWebhook` con url + secret_token + allowed_updates[message,callback_query] ✅. **Bot vivo: `@Asis_Inventario_bot`** (getWebhookInfo sin errores, 0 pendientes). Smoke prod: salud 200 + noindex · webhook sin/mal secret 401 · bandeja sin auth 401.
  - **Licencia Meta de visión — RESUELTA (B9.6, Version `bed68138`):** `correrVision` se auto-sana — si la 1ª llamada falla (típicamente por licencia sin aceptar), acepta `prompt:"agree"` y reintenta una vez; la aceptación queda en la cuenta. Cero paso manual en el dashboard; el primer OCR ya funciona. Para operar: código de vinculación en `#/recepciones-pendientes` → la persona escribe `vincular NNNNNN` al bot → `nuevo`.
- [x] **S7:** B10.1 grabadora + ingesta R2 + Whisper — 🚧 **GATE PARCIAL VERDE** (2026-07-08, typecheck + tests parciales; el GATE B10 completo es S8):
  - [x] B10.1.1 Migración `0004_audio.sql`: `audio_grabacion` (plan D1 §5.4 TAL CUAL) + `audio_senal` (§4.3, **veto D-N5 horneado: SIN operador_id**). Aplicada local (0004 ✅). En S7 solo se usa `audio_grabacion`.
  - [x] B10.1.2 `lib/whisper.ts` — `transcribirBytes` (Whisper `@cf/openai/whisper-large-v3-turbo`, **audio en base64 + `language:"es"` + `vad_filter`**, salida `.text`; **verificado contra los docs de Cloudflare 2026-07**). Guardado como `vision.ts` (sin binding/`AI` → null; el orquestador marca 'error').
  - [x] B10.1.3 `repos/audio.ts` — `audioRepo(db,actor)` ingesta scoped (INSERT OR IGNORE, idempotente por client_uuid) · `audioSistemaRepo(db)` (obtener/pendientes/guardar/error, transiciones idempotentes por guarda de estado) · `transcribirAudio` (lee R2 → transcribe → persiste; **transcriptor inyectable** para no llamar IA real en tests) · `barrerAudios`/`barrerAudiosPendientes` (barredora del Cron; `env.DB` vive AQUÍ, no en worker.ts).
  - [x] B10.1.4 `repos/dispositivo.ts` — provisión de grabadores A10 (token en claro UNA vez, solo hash en D1) + `listar` + `activar` (kill-switch), scoped por tenant/sucursal.
  - [x] B10.1.5 `mw requiereDispositivo` + rutas: `POST /api/audio` (auth de DISPOSITIVO, cuerpo crudo + metadatos por query → R2 `audio/{tenant}/{fecha}/{uuid}.webm` + fila 'subido' + `ctx.waitUntil(transcribir)` con try/catch para tests) · `GET /api/audio` (recientes, admin+) · `GET/POST/PATCH /api/dispositivos` (admin+).
  - [x] B10.1.6 `src/worker.ts` = `{ fetch: app.fetch, scheduled }` (Cron barredora). **`wrangler.jsonc`: `main`→`src/worker.ts` + `"triggers":{"crons":["*/5 * * * *"]}`.** `index.ts` sigue exportando el app de Hono por defecto → los 9 tests que hacen `import app from "../src/index"` NO se tocan. **Canal prohibido (#14) intacto** (env.DB/SQL solo en repos/).
  - [x] B10.1.7 Grabadora PWA `#/grabadora` (modo dispositivo, sin sesión de usuario; token en localStorage) — MediaRecorder opus ~32 kbps en chunks de 30 s con **grabador ROTATIVO** (stop→start por chunk = webm completo y decodificable por Whisper, no fragmento), descarte de silencio por RMS, wake lock. Cola Dexie aparte (FIFO **200 MB** idempotente por client_uuid) + flusher de reintento offline (modo avión→online). Lógica pura testeada; APIs de navegador = T-K2.
  - [x] 🚧 **GATE PARCIAL:** typecheck api+pwa · **API 107/107** (15 nuevos audio: provisión, ingesta, idempotencia, cuerpo vacío/uuid inválido, no-dispositivo=403, transcripción con fake, transición idempotente, error sin-R2/null, barredora + corte de antigüedad, panel + **aislamiento D-N8**) · **PWA 19/19** (14 nuevos: RMS/silencio/evicción FIFO + cola/flush/backoff/subidor) · build SPA · migración local. Commits `a71943b` (API) · `8d687b5` (PWA). **DESPLEGADO a prod con OK de Kevin — Version `656554e7`** (2026-07-08): migración 0004 remota ✅ · `wrangler deploy` (cron `*/5 * * * *` registrado; bindings DB/MEDIA/AI/ASSETS) ✅ · **lifecycle R2 `audio-7d` = expira 7 días solo el prefijo `audio/`** (verificado; el `bot/` NO tiene regla de expiración) ✅ · push origin (`1d78f9c..4ed83a6`) ✅. Smoke prod: salud 200 + `X-Robots-Tag noindex` · `POST /api/audio` sin token 401 · `/api/dispositivos` sin auth 401 · SPA con meta noindex. **Grabadora física en el A10 = T-K2** (la ruta `#/grabadora` ya está viva; falta pegar un token de dispositivo generado en `#/grabadores` y validar mic/wake lock en el teléfono).
  - [x] B10.1.8 (seguimiento 2026-07-08, **Version `bbfb8ee5`**) — pedido de Kevin para dejar el A10 desatendido: (a) **panel admin `#/grabadores`** (admin+, selector de sucursal para super): crea grabador → muestra el token EN CLARO una vez con botón copiar + kill-switch por grabador (sin esto no había forma de provisionar el A10 desde la UI). (b) **Grabadora endurecida** para operar sola tras el único toque de "Iniciar": watchdog que reinicia la captura si se estanca (>45 s sin chunk), reinicio en `MediaRecorder.onerror`, **intención persistida** en localStorage → tras corte/recarga reanuda sola (y si el navegador exige gesto, botón grande "Reanudar" de un toque), degradación del filtro de silencio si el AudioContext no corre (sube de más antes que perder), latido "última captura hace Ns". PWA 19/19 · typecheck · build · smoke prod (SPA 200/noindex, `/api/dispositivos` 401). Commit `2f02438`. **Límite honesto (no se puede saltar):** una web NO graba con la pantalla bloqueada/apagada (el navegador corta el micrófono); el A10 va como kiosco (enchufado, pantalla siempre encendida, app fijada, sin auto-bloqueo). El "todo apagado" real = grabador dedicado a futuro (mini-PC / audio de las cámaras Hikvision / Raspberry → mismo `/api/audio`).
  - [x] B10.1.9 **FIX de autenticación del token (lo detectó Kevin) — Version `68142185`, commit `3fa60f1`:** la grabadora abierta en el MISMO navegador donde hay un admin logueado arrastraba sola la **cookie de sesión**; `extraerToken` (`mw/auth.ts`) daba **precedencia a la cookie sobre el header Authorization**, así que `/api/audio` resolvía al USUARIO admin (no al dispositivo) → **403 "token rechazado"** (en un teléfono aparte, sin cookie, sí funcionaba). Dos capas: **(A) server** — un Bearer explícito manda sobre la cookie ambiental (la cookie queda de respaldo para navegación directa); **(B) cliente** — la grabadora sube con `credentials:"omit"` para no arrastrar ninguna cookie. Regresión en `audio.test.ts`: cookie de admin + Bearer de dispositivo → 201 registrado como el grabador; cookie sola sin Bearer → sigue 403. API **109/109** · typecheck · smoke prod. **GOTCHA DURABLE:** cualquier auth de DISPOSITIVO por token (grabador A10, futuras cámaras Hikvision) debe autenticar por el header, no por la cookie ambiental — el Bearer explícito manda.
- [x] **S8:** B10.2 extracción de señales + bandeja + test del veto — 🚧 **GATE B10 VERDE** (2026-07-09):
  - [x] B10.2.1 `lib/senales.ts` — extractor del audio: `@cf/meta/llama-3.1-8b-instruct` (instruct chico, id + `messages`/`temperature` verificados contra docs CF 2026-07), prompt CERRADO a JSON → `{faltantes:[{nombre,cantidad,confianza}], ventas:[{items,confianza}]}`. Guardado (null ante fallo) e **INYECTABLE** (tests con fake → nunca IA real). D-N5: la salida solo trae nombres de producto + confianza, jamás operador.
  - [x] B10.2.2 `repos/audio.ts` — `procesarSenales` (transcrito→procesado): **faltantes** matcheados por FTS contra el catálogo del tenant (tokens AND, fallback al token más largo; primero-que-pega-gana, el humano confirma) → una señal por producto; **venta_posible** con cotejo ±10 min contra las ventas POS de la sucursal (`estado='completada'`, por sucursal, NUNCA por operador) → `autocerrado` si hay venta, `pendiente` si no. Ids deterministas `${audioId}-f/v${i}` + INSERT OR IGNORE → idempotente. `procesarAudio` encadena transcripción+señales en el `waitUntil` de la ingesta (<2 min); `barrerSenales` corre en el Cron tras `barrerAudios`. `audioSenalRepo`: bandeja `pendientes` (enriquecida con el nombre de catálogo) + `confirmar` (**faltante → quiebre REAL** vía `quiebreRepo`, `client_uuid='senal:<id>'` idempotente, **operador_id NULL** — el faltante lo dijo el ambiente, D-N5) + `descartar`. Rutas `GET/POST /api/audio/senales/*` (**operador+**, scoped por sucursal verificada). Todo el SQL en repos/ (#14 intacto). Sin migración nueva (0004 ya creó `audio_senal` en S7, local+remota).
  - [x] B10.2.3 tests — `audio.test.ts` (+13): faltante 'no hay X' → señal con match FTS del tenant · sin match → producto_id null · sin faltante/venta → 0 señales pero 'procesado' · reproceso idempotente · **venta_posible autocierra** con venta POS en la ventana (y NO con venta de otra sucursal) · bandeja `GET` con nombre de catálogo · **confirmar → quiebre real SIN operador** + liga quiebre_id + sale de la bandeja · confirmar 2× idempotente (un solo quiebre) · descartar · **aislamiento cross-sucursal/tenant (404)** · barredora del Cron. `veto-audio.test.ts` (+3, **TEST DEL VETO D-N5** tipo canal-prohibido): `audio_senal` sin columna operador/usuario (PRAGMA) · ninguna query que toque `audio_senal` cruza con personal (operador/usuario_perfil) · solo `repos/audio.ts` la lee (el futuro repo de casos/EBR S9 NO puede).
  - [x] B10.2.4 PWA — `useSenales` (polling online 20 s de las señales pendientes de la sucursal; no bloquea la venta, que va por Dexie) + `SenalesBandeja.tsx` (faltantes/ventas con producto matcheado + confianza; confirmar/descartar optimistas) + Mostrador **badge `🎙️ N`** que abre la bandeja (solo con sucursal; el super no cobra ahí).
  - [x] B10.2.5 **FIX CRÍTICO (validando la extracción contra Workers AI REAL — lo que los tests con fake NUNCA podían ver):** (1) **`const run = env.AI.run; run(...)` DETACHA el método y pierde `this`** → lanza `Cannot set properties of undefined (#options)` → siempre null. **Afectaba también `whisper.ts` (S7 transcripción) y `vision.ts` (S6 OCR)**, cuyo camino real jamás se ejerció en vivo (gate con fakes; T-K2/T-K12 pendientes) → **la transcripción y el OCR estaban ROTOS en prod**. Fix: llamar `ai.run(...)` como MÉTODO en las 3 libs. (2) Con `messages`, los Llama actuales devuelven el texto en `choices[].message.content` y traen un `response` **vacío** → leer `response ?? choices` daba `""`→null; ahora se toma el primer candidato no-vacío. (3) **`@cf/meta/llama-3.1-8b-instruct` (base) quedó DEPRECADO el 2026-05-30 (error 5028)** → modelo elegido validando en vivo los 3 casos + mixto en español: **`@cf/meta/llama-3.2-3b-instruct`** (acertó todo sin falsos positivos; mejor que 3.1-8b-fp8 y llama-4-scout). Extracción **verificada end-to-end contra Workers AI real**. Commit `24320b5`.
  - [x] 🚧 **GATE B10 VERDE:** **API 125/125** (13 audio B10.2 + 3 veto D-N5) · **PWA 19/19** · typecheck api+pwa · **build SPA**. Commits `0f954b5` (extractor) · `1e8ef3a` (pipeline+rutas) · `4e326da` (tests+veto) · `e4367d6` (PWA) · `c22a535` (docs) · `24320b5` (fix this-binding + modelo). **DESPLEGADO A PROD con OK de Kevin (2026-07-09) — Version `7a7de524`:** build SPA + stage `public` + `wrangler deploy` (bindings DB/MEDIA/AI/ASSETS + cron `*/5` en vivo) + push origin (`011041c..ef9aabf`). Smoke prod verde: salud 200 · `X-Robots-Tag noindex` + meta noindex · `/api/audio`, `/api/audio/senales`, `/api/dispositivos` sin token → 401. **Este deploy también arregla la transcripción (S7) y el OCR del bot (S6)**, que estaban rotas en prod por el bug del `this`.
  - [x] **T-K2 VERIFICADO en vivo (2026-07-09):** el A10 ("A10 - Samsung", sucursal VES) provisionó grabador, subió audio, transcribió y **generó señales** en prod (pipeline end-to-end OK). La verificación server-side (D1 remota) mostró los defectos de calidad que Kevin anticipó → motivaron B10.2.6: Whisper masacra nombres de medicamentos ("fenazopiridina"→"penasopiridina") y colaba conversación ajena ("chancho/mariscos/pescado", confianza 0). **Pendiente: re-test con audio nuevo tras B10.2.6** (el teléfono paró de capturar 23:52; el sesgo/filtro se validan con voz fresca).
  - [x] B10.2.6 **Sesgo de transcripción + filtro de ruido — DESPLEGADO Version `d6200d06`:** `transcribirBytes` acepta `initial_prompt` (verificado en docs CF); `transcribirAudio` lo arma por tenant = jerga de botica/monedas (soles, vuelto, sencillo, cambio…) + hasta 40 nombres del catálogo del tenant, acotado a ~200 tokens (los precios NO van — números, se resuelven en D1). `UMBRAL_SENAL=0.4` → no se crea señal de baja confianza; prompt de extracción exige SOLO productos de botica (ignora comida/temas ajenos). API 128/128 · typecheck. Commit `a7be4e9`.
- [x] **S8.5: B10.3 optimización de calidad del audio — 🚧 GATE VERDE (2026-07-10, Opus 4.8 máx+1M), commits `96e1415` (backend) + `e257c8f` (PWA); LOCAL, deploy pendiente OK Kevin:**
  - [x] B10.3.1 **diccionario de correcciones que APRENDE** (migración `0005` tabla `audio_correccion`, tenant-scoped, **SIN operador — VETO D-N5**, patrón `producto_alias`): `matchProductoTenant` consulta la corrección ANTES del FTS ("penazopiridina"→Fenazopiridina de una); **confirmar auto-aprende** cuando el faltante ya matcheó + acepta override `producto_id` ("corregir" el producto correcto → va al quiebre Y se aprende); `audioCorreccionRepo` listar/**enseñar** (re-matchea las señales pendientes que calzan)/borrar; rutas `GET/POST/DELETE /api/audio/correcciones` (admin+). **Test del veto extendido**: `audio_correccion` tampoco lleva personal.
  - [x] B10.3.2 **reporte de calidad diario** (migración `0005` tabla `audio_reporte_calidad`, PK sucursal+fecha): `actualizarReporteCalidad` upserta el snapshot de hoy por sucursal (lo corre el Cron tras barrer); `GET /api/audio/calidad?sucursal_id=&dias=` (admin+) = **hoy en vivo** + días previos del snapshot + faltantes pendientes **sin match** (para enseñar) + **errores** recientes. Panel PWA `#/audio-calidad`.
  - [x] B10.3.3 **`sin_habla` + frecuencia de venta**: `transcribirBytes` devuelve resultado **discriminado** (`texto`/`sin_habla`/`error`) → chunks que Whisper procesó pero sin voz van a **`estado='procesado'` + `sin_habla=1`** (flag; terminal benigno) en vez de 'error' (ya no ensucian el panel). `nombresCatalogoTenant` ordena por **corrección > frecuencia de venta > alfabético** (antes solo alfabético) → el sesgo de Whisper se gasta en lo que más importa.
  - [x] **GOTCHA DURABLE (lo atrapó el deploy, no los tests): la D1 REMOTA enforcea la FK `audio_senal→audio_grabacion` y NO respeta `PRAGMA defer_foreign_keys` en `wrangler d1 migrations apply` → un rebuild 12-step de una tabla REFERENCIADA revienta (D1 auto-rollback, remoto queda intacto). Miniflare local es más laxo (por eso el gate no lo vio).** El CHECK de estado no admite ALTER, así que `sin_habla` se resolvió con **`ALTER TABLE ADD COLUMN` (FK-safe)**, no reconstruyendo la tabla. Regla: en D1, evitar rebuild de tablas referenciadas por FK; preferir ADD COLUMN / flags.
  - [x] 🚧 **GATE VERDE + DESPLEGADO (OK de Kevin, 2026-07-10) — Version `eb04b67f`:** **shared 160/160 · API 140/140** (10 B10.3 + veto extendido) · **PWA 19/19** · typecheck api+pwa · **build SPA**. Migración `0005` aplicada a la D1 remota ✅ (2 tablas + `sin_habla` col; 5 comandos). `wrangler deploy` (bindings DB/MEDIA/AI/ASSETS + cron `*/5`). Smoke prod: salud 200 · `X-Robots-Tag`+meta noindex · `/api/audio/calidad` y `/api/audio/correcciones` sin token → 401. **Pendiente Kevin: re-test del sesgo con voz fresca** (ahora visible en el panel `#/audio-calidad`) + usar el bucle de "corregir nombres".
- [x] **S8.6 = B10.4 (contexto y auditoría del audio para el piloto de 3 meses) — 🚧 GATE VERDE + revisión adversarial 5 lentes + DESPLEGADO (OK de Kevin) — Version `6d9ab805`, 2026-07-11 (Opus 4.8 máx+1M). Commits `42a64c8` (backend) + `cbb8c1e` (PWA) + `2b27e92` (docs), pusheados a origin. DECISIÓN LPDP de Kevin: retención 30 días (lifecycle R2 `audio-30d` aplicado).**
  - [x] B10.4.1 **Catálogo de prueba** (`repos/catalogo-prueba.ts` + rutas `/catalogo/prueba/{conteo,cargar,purgar}` + pantalla `#/catalogo-prueba`): promueve `catalogo_maestro` (medicamentos) → producto + presentación base + código + `inventario_local` 100u + lote + precio placeholder (`sinIgvDmDesdeVentaPublicaDm`), **marcado `es_prueba=1`** (migr `0006`, ADD COLUMN). Carga **paginada por keyset** (cursor `siguiente_desde`, barra de progreso, cancelable), **idempotente por GTIN**, **resiliente por producto** (try/catch), purgable por flag. Alcance elegible (todo / primeros N) al ejecutar.
  - [x] B10.4.2 **Contexto de la señal**: columna `audio_senal.frase` (migr `0006`) + helper puro `fraseFuente()` (elige la oración con el producto, determinista) + `pendientes()`/`reporteCalidad()` hacen JOIN a `audio_grabacion` → frase + transcripción completa + `audio_id`. Surface en la bandeja del Mostrador (frase + "ver transcripción completa") y en `#/audio-calidad`.
  - [x] B10.4.3 **Reproductor + retención**: `audioMediaRepo` + `GET /audio/:id/media` (proxy R2, **admin+ scoped por sucursal** — como el proxy de fotos) + `AudioPlayer` (Bearer→blob→objectURL) en el panel. **Purga total** `POST /audio/purgar` (solo super: borra R2 + `audio_grabacion`/`audio_senal`, **conserva quiebres**) = salvaguarda LPDP para cerrar la prueba. Retención 7→**30 días** = `wrangler r2 bucket lifecycle` al deploy (OK de Kevin).
  - [x] B10.4.4 **Feedback activo**: `confirmar` acepta `producto_id` (Corregir → quiebre + corrección aprendida de B10.3) + aclaración horneada "confirmar venta posible **NO** registra una venta".
  - [x] **Revisión adversarial (workflow 5 lentes → 10 hallazgos, 8 confirmados y CORREGIDOS con tests):** **(ALTO) IDOR intra-tenant** — el proxy de audio scopeaba solo por tenant → un admin_sucursal oía audio de OTRA sucursal (D-N8); ahora `claveAudio` exige la sucursal para no-super. **(ALTO) Límite de 100 binds/query de la D1 REMOTA** — `gtinsExistentes` ligaba tenant+100 GTIN = 101 → reventaba en prod (invisible en miniflare local, como la asimetría de FKs del ADR-011); troceo 100→99 aquí + en `importar-catalogo` y `pedido` (patrón latente en herramientas ya en prod). **(MEDIO) Reporte de calidad agrupaba por día UTC** no Lima → mal-fechaba las señales de la tarde/noche; ahora `fechaLocal()` + `date(created_at,'-5 hours')` como caja/dashboard. **(MEDIO/BAJO×5)** banners de error en rojo (antes verde de éxito, incl. purga LPDP fallida), fuga de objectURL del `AudioPlayer` al desmontar, botón Reintentar, y `borrar` corrección ya no traga el error.
  - [x] 🚧 **GATE B10.4:** **API 158/158** (audio 53, catalogo-prueba 9 con test de 101 filas/2 trozos + cross-sucursal) · **PWA 19/19** · shared 160 · typecheck api+pwa · build SPA · **veto D-N5 intacto** (4/4). **DESPLEGADO (OK de Kevin, 2026-07-11) — Version `6d9ab805`:** `d1 migrations apply --remote` (0006, 3 comandos) + **`wrangler r2 bucket lifecycle` remove `audio-7d` / add `audio-30d` prefijo `audio/`** + build SPA + stage `public` + `wrangler deploy` (bindings DB/MEDIA/AI/ASSETS + cron `*/5`) + push origin (`1b76519..2b27e92`). Smoke prod verde: salud 200 · X-Robots-Tag+meta noindex · `/api/audio/:id/media`, `/api/audio/purgar`, `/api/catalogo/prueba/{conteo,cargar}` sin token → 401. **Falta (Kevin, no bloquea): cartel de aviso LPDP en el mostrador + cargar el catálogo de prueba desde `#/catalogo-prueba` + eyeball del reproductor.**
- [x] **S9 = B11 (EBR determinista + espejo operativo) — 🚧 GATE B11 VERDE + revisión adversarial 5 lentes + DESPLEGADO A PROD (2026-07-11, Opus 4.8 máx+1M). Version `c81c4e47` (B11) + `9888742e` (B11.1b no_sale). Migr 0007 remota + `wrangler deploy` (2 crons: `*/5` audio + `0 8` EBR) + push origin (`7d53687..d60114d`). Smoke prod verde (salud 200/noindex; `/api/casos`, `/api/casos/espejo`, `/api/eventos-caja/no-sale` sin token → 401). Commits `2f3a606` (backend) + `b93a4f7` (PWA) + `46d0c54` (docs) + `d60114d` (B11.1b).**
  - [x] B11.1 **EBR + bandeja de casos** (`repos/casos.ts` + migración `0007_casos.sql`): 6 indicadores deterministas sobre datos POS — `cajon_sin_venta` (Δ3), `anulaciones` (ratio ≥2× promedio botica 30d), `venta_bajo_precio` (recomputado vs `precio_local` vigente-al-momento, ya que la advertencia del batch NO se persiste en audit_log), `descuadre` (`cierre_caja`: grande en un día o recurrente 30d), `stock_negativo` (`inventario_local`<0), `fuera_horario` (hora Lima vs horario de la sucursal). `barrerCasos` = Cron nocturno `"0 8 * * *"` (03:00 Lima) con **autocierre a 14 días**, **idempotencia por `clave_dedup`** (índice UNIQUE + INSERT OR IGNORE), tope anti-fatiga (40/corrida, más severos primero) y **catch-up de 2 días** para los indicadores diarios (si el Cron se salta una noche). Bandeja `#/casos` con **aislamiento por sucursal** (super elige `?sucursal_id` verificado por tenant); confirmar/descartar registra revisor+estado. **Umbrales en constantes exportadas (ajustables sin migración).**
  - [x] B11.2 **Espejo operativo** (`repos/espejo.ts` + pestaña "Personal"): ventas/hora, ticket, tiempo de servicio (Δ2 `atencion_inicio`→cobro vía `julianday`), anulaciones, cajón, faltante por operador **vs el promedio de la MISMA botica** (nunca cross-botica). Informa; jamás sanciona (los umbrales abren `caso` tipo `personal`, un humano decide).
  - [x] B11.3 **clips** DIFERIDO (T-K3 ⬜, modelo Hikvision): gancho dejado en la tarjeta del caso, sin bloquear B11.1-2.
  - [x] **Horario de sucursal** por `ADD COLUMN hora_apertura/hora_cierre` (FK-safe, gotcha D1) + editable en el form de Sucursales. NULL = indicador `fuera_horario` apagado para esa botica (cero falsos positivos).
  - [x] **VETO D-N5**: `repos/casos.ts`/`repos/espejo.ts` NUNCA leen tablas de audio. Test extendido (regex ancha `\baudio_\w+` + prohíbe importar `repos/audio`) — cierra el laundering cross-módulo. **Sanción automática nunca.**
  - [x] **B11.1b — acción `no_sale` (activa `cajon_sin_venta`, decisión de Kevin):** el POS ahora emite `evento_caja` tipo `no_sale` vía el botón "🔓 Cajón" del Mostrador (`repos/evento-caja.ts` + `POST /api/eventos-caja/no-sale`, idempotente por client_uuid, offline por la cola Dexie, `sucursalVerificada`). El indicador **deja de estar dormido**: detecta frecuencia anómala de aperturas DECLARADAS por operador. **No es fraude-proof** (un deshonesto no pulsa el botón); la señal fraude-proof `apertura_sin_venta` = sensor físico del cajón/impresora (**T-K1**), que entra por el mismo indicador cuando llegue el hardware. Tests: 201/200 idempotente + shape + 400 sin uuid. **`merma_conteo` (→C3) y `ticket_sin_impresion` (→T-K1) siguen diferidos.**
  - [x] 🚧 **GATE B11 VERDE:** shared **168/168** (8 horario) · **API 182/182** (19 casos + 2 no_sale + veto D-N5 **6/6**) · PWA 19/19 · typecheck 5/5 · build SPA. **Revisión adversarial (workflow 5 lentes → 8 hallazgos, 7 confirmados y CORREGIDOS con verificación; aislamiento salió LIMPIO; el hallazgo de límite de subrequests fue REFUTADO — Cloudflare subió el tope a 10.000 en feb-2026):** (MEDIO) `venta_bajo_precio` ignoraba la cantidad → subvaluaba pérdida/severidad; ahora × `cantidad_presentacion` (el precio es por presentación). (MEDIO) `cajon_sin_venta` presentado como activo sin fuente → **resuelto por B11.1b (acción no_sale)**. (BAJO) indicadores diarios sin catch-up si el Cron falla → ventana de 2 días idempotente. (BAJO×2) test del veto evadible por tabla audio nueva / import de audio → regex ancha + prohibición de import. (BAJO) "limpiar" horario no persistía + `created_at` mostrado en UTC → corregidos. **Pendiente Kevin (no bloquea; el deploy YA se hizo): (1) cargar el horario de VES en `#/sucursales` para activar `fuera_horario`; (2) T-K1 (impresora/sensor de cajón) para la señal fraude-proof `apertura_sin_venta` y el indicador `ticket_sin_impresion`; (3) datos POS reales (T-K4/T-K5 catálogo+usuarios) para que el EBR opere sobre movimiento real, no seed sintético.**

## Tareas de Kevin (no bloquean el arranque; bloquean lo indicado)

| # | Tarea | Bloquea | Estado |
|---|---|---|---|
| T-K1 | Impresora térmica VES conectada + sesión WebUSB | E9 | ⬜ |
| T-K4 | Catálogo real VES (~100–300 SKUs: nombre, barras, precios, stock) | E12.4 | ⬜ (importador EN PROD 06-jul, `#/importar-catalogo`; falta SOLO la hoja real) |
| T-K5 | Emails de papá y mamá para sus cuentas admin | E12.4 | ⬜ |
| T-K6 | Ratificar D9 (admin en la SPA) — **ratificada por defecto si no dice lo contrario** | — | ⬜ |
| T-K2 | Instalar PWA grabadora en el A10 | P2 | ⬜ |
| T-K3 | Modelo exacto Hikvision + acceso a config | P3/P6 | ⬜ |
| T-K7 | Báscula contadora (VES primero) | P5-C4 | ⬜ |
| T-K8 | Curar 5–10 reglas de venta cruzada | P4a | ⬜ |
| T-K9 | WhatsApp emisor por botica | P4a | ⬜ |
| T-K10 | Marcar SKUs crónicos + dosis | P4a | ⬜ |
| T-K11 | Listas vigentes de las 3–4 droguerías (CSV/Excel) + monto mínimo, flete y días de entrega de cada una | e2e real de B8 | ⬜ |
| T-K12 | Crear bot en @BotFather + token por canal seguro (`wrangler secret`) + quiénes lo usan | smoke real de B9 | ⬜ |
| T-K13 | Umbral "vencimiento corto" (default 8 meses) + revisar combos del primer pedido real | afina B8 | ⬜ |

## Notas para S2 (leer antes de E5/E6)

- **Venta S1 es MINIMAL** (solo cabecera, scoped): `ventaRepo.crearCabeceraMinima` y `anularMinima` + rutas `POST /api/ventas`, `POST /api/ventas/:id/anular` en `routes/protegidas.ts`. **S2/E6 las REEMPLAZA** con el batch §7.3 tal cual (FEFO cascada, venta_item, venta_item_lote, movimiento, audit) y la anulación §7.6.
- **Δ1 en venta_item:** `presentacion_id` y `cantidad_presentacion` son **NOT NULL** en el esquema → el INSERT de venta_item del §7.3 debe incluirlas (cantidad base = cantidad_presentacion × factor). El selector de presentación entra en la UI (E5/E6).
- **Retry remoto:** usar `withRetry` (repos/base.ts) también en el lazo de reintento por CHECK del §7.4 (tolerar "Network connection lost").
- Endpoints ya existentes (scoped) que E5/E8/E10/E11 deben COMPLETAR (hoy son mínimos): precios (versionado vigente_hasta), inventario/ajustes, faltantes/consolidado (quiebres 14d + sugerido), usuarios, audit.
- Worker desplegado para el spike: `https://huayruro.k-alexander-m-g.workers.dev` (solo `/api/salud` + assets placeholder; ruta spike ya retirada).

## Bloqueado

- **ESLint no corre repo-wide** (ESLint 9 + config legacy `.eslintrc.cjs` + plugins @typescript-eslint no instalados; sin hooks git activos en `.husky/`). Deuda PREEXISTENTE, no de S1. El canal prohibido se enforce por el **test #14** (verde). Migrar a flat config es tarea aparte.
- ~~Seeds remotos + deploy prod~~ — **HECHO** (E12, 2026-07-06): D1 remota migrada + sembrada (seed sintético), worker desplegado con noindex. Prod verificado en vivo.
- ~~Capa de páginas React necesita navegador~~ — **HECHO** (S3): cutover + pantallas + retiro de Supabase completos y con gate verde. Ver B3/B4/B5.
- ~~E12 (deploy prod + GATE 4)~~ — **HECHO** (2026-07-06): Kevin autorizó el deploy; GATE 4 verde en vivo. Lección crítica: **Cloudflare Workers rechaza PBKDF2 >100 000 iteraciones en prod** (local no lo aplica) → tope 100k horneado con comentario anti-regresión en `src/lib/password.ts` y `scripts/gen-seed-d1.mjs`. Y el `X-Robots-Tag` del Worker no cubre los assets (SPA) → noindex por meta + `_headers`.
- ~~Retirar `apps/admin` + `git push`~~ — **HECHO** (2026-07-06, Kevin autorizó explícitamente tras el cierre): apps/admin retirado (`654a8f4`), rama `d1-rebuild` pusheada a origin. El clasificador los había denegado en la sesión de build por no estar nombrados; se hicieron con el OK explícito de Kevin.
- **T-K1 (impresora física VES)**: `impresora.ts` construido con fallback CSS funcional; la validación WebUSB real + el status de cajón (Δ3 `apertura_sin_venta`) siguen pendientes de la sesión física.
- **T-K4/T-K5 (catálogo y usuarios reales)**: siguen pendientes; el deploy piloto irá con el seed sintético.

## Log de sesiones

| Fecha | Bloque | Resultado | Commit(s) |
|---|---|---|---|
| 2026-07-11 | S9 (B11) | **🚧 GATE B11 VERDE (local) + revisión adversarial 5 lentes. EBR determinista + espejo operativo + bandeja de casos.** (1) **EBR** `repos/casos.ts` + migr `0007`: 6 indicadores sobre datos POS (cajón sin venta [DORMIDO: falta fuente no_sale], anulaciones ratio 2×, venta bajo precio recomputada vs precio vigente, descuadre grande/recurrente, stock negativo, fuera de horario). `barrerCasos` = Cron nocturno `"0 8 * * *"` (03:00 Lima) con autocierre 14d, idempotencia por `clave_dedup` (UNIQUE + INSERT OR IGNORE), tope 40/corrida y catch-up de 2 días para los diarios. Bandeja `#/casos` con aislamiento por sucursal + confirmar/descartar con revisor. (2) **Espejo** `repos/espejo.ts` + pestaña Personal: métricas por operador vs promedio de la misma botica (ventas/hora, ticket, servicio Δ2, anulaciones, cajón, faltante). Informa, no sanciona. (3) **Horario** de sucursal por ADD COLUMN (FK-safe) + form editable → activa `fuera_horario`. (4) **VETO D-N5** extendido (casos/espejo no leen audio: regex `\baudio_\w+` + prohíbe import de repos/audio). B11.3 clips DIFERIDO (T-K3). **Revisión adversarial 5 lentes → 8 hallazgos, 7 corregidos** (aislamiento LIMPIO; subrequests REFUTADO): venta_bajo_precio × cantidad_presentacion, catch-up diario, veto endurecido, limpiar-horario persiste, fecha en hora Lima. **B11.1b: acción `no_sale` (botón 🔓 Cajón → evento_caja no_sale, offline; activa cajon_sin_venta; apertura_sin_venta fraude-proof sigue en T-K1).** **shared 168 · API 182 (28 casos+veto+no_sale, veto 6/6) · PWA 19 · typecheck · build SPA.** **DESPLEGADO A PROD (OK de Kevin) — Version `c81c4e47` (B11) + `9888742e` (no_sale):** 0007 remota + `wrangler deploy` (2 crons) + push origin (`7d53687..d60114d`); smoke verde (salud 200/noindex, /casos + /espejo + /eventos-caja/no-sale 401). Falta Kevin (no bloquea): horario VES + T-K1. | 2f3a606 (backend) · b93a4f7 (PWA) · 46d0c54 (docs) · d60114d (no_sale) |
| 2026-07-11 | S8.6 (B10.4) | **🚧 GATE VERDE (local) + revisión adversarial 5 lentes. Contexto y auditoría del audio para el piloto de 3 meses.** (1) **Catálogo de PRUEBA** (`repos/catalogo-prueba.ts` + `/catalogo/prueba/*` + `#/catalogo-prueba`): promueve el maestro (medicamentos) → producto+100u+lote+precio placeholder, `es_prueba=1`, paginado keyset/idempotente/purgable. (2) **Contexto**: `audio_senal.frase` + `fraseFuente()` + JOIN a `audio_grabacion` → frase+transcripción+audio_id en bandeja y `#/audio-calidad`. (3) **Reproductor**: `GET /audio/:id/media` (proxy R2, admin+ scoped por sucursal) + `AudioPlayer` + **purga total** `/audio/purgar` (super, conserva quiebres). (4) **Feedback**: confirmar con `producto_id` (Corregir) + aclaración venta_posible≠venta. Migr `0006` (ADD COLUMN `frase`+`es_prueba`, FK-safe). **Decisión LPDP de Kevin: retención 30 días.** **Revisión adversarial → 8 fixes:** IDOR intra-tenant del proxy (D-N8 por sucursal), **límite 100 binds/query D1 remota** (troceo 99, +importar-catalogo +pedido), día Lima en el reporte, banners de error rojos, fuga objectURL, Reintentar, borrar-sin-tragar. **API 158/158 · PWA 19/19 · shared 160 · typecheck · build · veto 4/4.** **DESPLEGADO (OK de Kevin) — Version `6d9ab805`:** 0006 remota + lifecycle R2 `audio-30d` + deploy + push origin (`1b76519..2b27e92`); smoke prod verde (salud 200/noindex/401×4). | 42a64c8 (backend) · cbb8c1e (PWA) · 2b27e92 (docs) |
| 2026-07-10 | S8.5 (B10.3) | **GATE VERDE + DESPLEGADO (OK de Kevin) — Version `eb04b67f`. Optimización de calidad del audio.** (1) **Correcciones que APRENDEN** (`audio_correccion`, tenant-scoped SIN operador D-N5): match consulta corrección antes del FTS, confirmar auto-aprende + override "corregir", panel enseña/borra + re-matchea pendientes. (2) **Reporte de calidad diario** (`audio_reporte_calidad` upsert por Cron; `GET /audio/calidad` hoy-en-vivo + snapshot + sin-match + errores; panel `#/audio-calidad`). (3) **`sin_habla`** (Whisper corrió sin voz → estado 'procesado' + flag `sin_habla=1`, ya no 'error') + **`nombresCatalogoTenant` por corrección > frecuencia de venta > alfabético**. Migración `0005` (2 tablas + `sin_habla` col). Test del veto extendido. **GOTCHA: el rebuild 12-step del CHECK revienta en la D1 REMOTA (enforcea FK audio_senal→audio_grabacion, no respeta defer_foreign_keys en migrations apply) — miniflare local no lo vio; se resolvió con ADD COLUMN FK-safe.** **shared 160/160 · API 140/140** (10 B10.3) **· PWA 19/19 · typecheck · build SPA.** Smoke prod verde (salud 200/noindex/401). | 96e1415 (backend) · e257c8f (PWA) · 73b4211 (fix FK-safe) |
| 2026-07-09 | S8 (B10.2) | **GATE B10 VERDE.** Extracción de señales del audio → bandeja del Mostrador → cierre del loop de abastecimiento. `lib/senales.ts` (LLM chico `@cf/meta/llama-3.1-8b-instruct`, prompt cerrado, guarded+inyectable) saca faltantes ("no hay X") y ventas posibles de la transcripción; `procesarSenales` matchea faltantes por FTS del tenant y cotea venta_posible ±10 min contra las ventas POS (autocierra si ya existe), idempotente por id determinista; `audioSenalRepo` bandeja + **confirmar faltante → quiebre REAL** (reusa quiebreRepo, **operador_id NULL**, D-N5) + descartar; `procesarAudio` transcribe+extrae en el waitUntil (<2 min) y `barrerSenales` en el Cron. Mostrador con **badge 🎙️** + bandeja (useSenales polling + SenalesBandeja). **TEST DEL VETO D-N5** (tipo canal-prohibido): audio_senal sin operador (PRAGMA), ninguna query cruza audio↔personal, solo repos/audio.ts la lee. API **125/125** (13 B10.2 + 3 veto) · PWA 19/19 · typecheck · build SPA. **Sin migración nueva** (0004 ya en remota desde S7). **Fix crítico B10.2.5 (validando IA real): `env.AI.run` detachado perdía `this` → transcripción (S7) y OCR (S6) ROTAS en prod; modelo base deprecado → `llama-3.2-3b`. DESPLEGADO Version `7a7de524` (arregla también S6/S7) + push origin; smoke verde. **T-K2 VERIFICADO en vivo (A10 sube→transcribe→señala end-to-end en prod).** B10.2.6 sesgo de transcripción (`initial_prompt`) + filtro de ruido (Version `d6200d06`). API 128/128. SIG: B10.3 optimización de calidad (correcciones aprendidas + reporte).** | 0f954b5 (extractor) · 1e8ef3a (pipeline+rutas) · 4e326da (tests+veto) · e4367d6 (PWA) · 24320b5 (fix this) · a7be4e9 (sesgo+filtro) |
| 2026-07-08 | S7 (B10.1) | **GATE PARCIAL B10.1 VERDE** (typecheck + tests parciales; GATE B10 completo = S8). Grabadora PWA A10 (`#/grabadora`, modo dispositivo sin login, MediaRecorder opus 30 s con grabador rotativo = webm decodificable por chunk, descarte de silencio RMS, wake lock, cola Dexie offline FIFO 200 MB idempotente) → `POST /api/audio` (auth de dispositivo, idempotente por client_uuid) → R2 + `audio_grabacion` 'subido' → Whisper turbo (base64+es, `ctx.waitUntil`, verificado contra docs CF) → 'transcrito'; **Cron `*/5` barredora** (worker.ts={fetch,scheduled}; index.ts intacto para los tests; env.DB solo en repos/). CRUD `/dispositivos` (token una vez + kill-switch). Migración 0004 (audio_grabacion §5.4 + audio_senal §4.3, **veto D-N5 sin operador_id**) aplicada local. API 107/107 (15 audio: ingesta/idempotencia/aislamiento D-N8/transcripción fake/barredora) · PWA 19/19 (14 grabadora) · build SPA. **DESPLEGADO a prod con OK de Kevin — Version `656554e7`** (0004 remota + deploy con cron `*/5` + lifecycle R2 `audio-7d` solo prefijo `audio/` + push origin; smoke verde). Falta física: T-K2 (token en el A10 + mic/wake lock). | a71943b (API) · 8d687b5 (PWA) · 4ed83a6 (docs) |
| 2026-07-07 | S6 (B9) | **GATE B9 VERDE.** Bot de Telegram de inventario (registra lo que LLEGA → bandeja de aprobación web → recepción REAL por el mismo kardex, D-N4). Webhook público con secret + allowlist dura por chat_id + dedup por update_id + máquina de estados sin framework + OCR guarded (visión) con fallback a texto + vinculación por código de 6 dígitos + bandeja admin+ con proxy de fotos R2 + alta al vuelo. shared 160/160 (15 conversación) · API 92/92 (13 bot: seguridad, dedup, aprobación con stock+lote FEFO, rechazo sin stock, aislamiento cross-sucursal/tenant, proxy R2) · PWA 5/5 · typecheck · build. Migración 0003 + bindings AI/R2. **DESPLEGADO a prod con OK de Kevin — Version `4135efa1`** (bucket R2 huayruro-media + migración 0003 remota + deploy + push origin); **bot ACTIVADO** (`@Asis_Inventario_bot`, secrets + webhook registrado, getWebhookInfo sin errores). Pendiente menor: licencia Meta de visión (fotos→texto hasta entonces). | 6b17174 (B9.1) · f543a89 (B9.2) · 1c49c10 (B9.3) · c790b01 (B9.4) · a0b4793 (B9.5) · f8ee4d0 (docs) |
| 2026-07-07 | S5 (B8.2–B8.4) | **GATE B8 VERDE.** Matching de listas (GTIN→alias→nombre→fuzzy token-set con guarda de concentración; confirmar APRENDE `producto_alias` → matchea sola después) + motor de pedido (precio efectivo con bonif en enteros, consolidación exacta ≤2 proveedores por enumeración con mínimos/fletes/venc-corto, top-3 con delta, CSV por proveedor) + UI (revisión de matching + página `#/pedido`). shared 170/170 (25 nuevos, golden a mano) · API 79/79 (9 comparador: aprendizaje, golden motor, CSV, aislamiento 2 tenants) · PWA 5/5 · typecheck · build · **smoke navegador real 0 errores** (pegar→matchear→confirmar 2 dudosos→pedido→CSV). **DEPLOY PROD Version `dc2743ae` + push a origin con OK de Kevin** (smoke prod: 200/401×3/noindex; S5 sin migraciones nuevas). | 196a19b (shared) · bab0828 (API) · 1955582 (UI) · a269f5a (docs) |
| 2026-07-07 | S4 (B7 + B8.1) | **GATE B7 VERDE.** Catálogo maestro SUSALUD 15,181 en D1 local (generador XLSX cero-deps, lotes 300 por límite 100 KB/statement) + FTS maestro + endpoints read-only + alta asistida con GTIN atómica + CRUD proveedores + ingesta de listas (CSV/TSV pegado, dry-run→commit atómico, factor/bonif/venc-corto parseados). shared 120/120 · API 70/70 (aislamiento 2 tenants) · PWA 5/5 · typecheck · build · smoke navegador 8/8. **D1 remota migrada+maestro cargado y DEPLOY PROD `c6ea9824` con OK de Kevin** (smoke prod: 200/401/noindex + e2e maestro). | bd0d092 (B7.1) · ac3b5c5 (B7.2) · a516a69 (API) · 498f43d (UI) |
| 2026-07-04 | S1 (B1+B2) | GATE 1-2 ✅ (60 golden) · GATE 3 ✅ (17/17, 14 aislamiento). Esquema+seeds local, auth+scoping, spike A/B/C/D verde local+remoto | 85ac071 (E0) · 4f2f4b2 (E2) · dd4bfbe (E1) · ffb9ddd (E3) · c23204f (E4) |
| 2026-07-04 | S2 (B3+B4) | GATE E6.3 ✅ (venta atómica, 9 tests) · GATE E7.2 ✅ (cola offline, cliente+D1). API completa de catálogo/venta/anulación/recepción/caja + fundación offline (Dexie/cola/flusher/banner/api/sync). API 32/32, PWA 5/5, typecheck limpio. Falta capa de páginas React (cutover Supabase→D1) → sesión con navegador | f3f3b70 (E5) · 1fcf301 (E6) · bc20874 (E6.3) · a827b14 (E8) · bedfc78 (E7.1/E5.2) · c641f64 (E7.2) |
| 2026-07-06 | Importador catálogo (T-K4) | **Importador de catálogo en lote CONSTRUIDO** (no desplegado; Kevin pidió "aún sin datos"). Parser CSV + validador + endpoint atómico `POST /catalogo/importar` (dry-run+commit) + purga demo + pantalla in-app super/admin. Dinero PVP→sin_igv reproduce el seed. Gate: typecheck · shared 106/106 · API 58/58 · build SPA · **smoke navegador 7/7**. **Revisión adversarial (5 lentes): 7 hallazgos corregidos con tests** (overflow que tumbaba el lote, coma-miles 1000×, comilla que comía filas, duplicado sin código, columna duplicada, base desactivada, inyección CSV, cross-tenant super) + 1 documentado (F8). Sin commit (pendiente decisión de Kevin). | *(sin commitear — working tree)* |
| 2026-07-06 (noche) | Planificación frentes nuevos (Fable 5) | **Importador COMMITEADO+pusheado** (`dcdd78a`; gate re-verificado: typecheck 5/5 · shared 106/106 · API 58/58). **Plan frentes nuevos ESCRITO** (`…-plan-frentes-nuevos.md`): B7 catálogo maestro (fuente VERIFICADA: SUSALUD GTIN v4, 15,181×14 cols, act. 11-abr-2025, XLSX descargado; EAN13 2021 complemento; OPPF 403 a bots; datos.minsa caído) · B8 comparador (matching GTIN→alias→nombre→fuzzy + aliases aprendidos; combos exactos ≤2 proveedores con mínimos/fletes/bonif/venc-corto) · B9 bot Telegram (webhook secret_token, allowlist, visión `@cf/meta/llama-3.2-11b-vision-instruct`, bandeja aprobación) · B10 audio (ingesta waitUntil <2 min, `audio_senal` sin operador_id, veto testeado) · B11 EBR+espejo+clips(T-K3). DDL 0002–0005 listo. Sesiones S4–S9 mapeadas. Fuentes del maestro commiteadas en `apps/api/seeds/fuentes/` (`5025e89`). **Deploy a prod con OK de Kevin: Version `ac6be920`** (smoke 200/401/noindex ✅). | dcdd78a (importador) · b5f89f8 (docs) · 5025e89 (fuentes) |
| 2026-07-06 | S3 (B6/E12) | **P0 PILOTO EN PROD** — Kevin autorizó el deploy. D1 remota migrada+sembrada, `wrangler deploy` → `huayruro.k-alexander-m-g.workers.dev` (noindex, `856ab18d`). **GATE 4 verde EN VIVO** (navegador real 1366×768 + 390×844, cero errores de consola; venta atómica S/1.80 end-to-end en prod). GATE 4 atrapó 2 bugs solo-prod: PBKDF2 >100k rechazado por Workers (→100k) y noindex ausente en la SPA (→meta+`_headers`); ambos corregidos y re-verificados. Suite API 39/39, typecheck, build SPA verdes. Kevin autorizó al cierre: apps/admin retirado + rama pusheada a origin. | 5796bc5 (fix) · 5ea5b00 (docs) · 654a8f4 (retiro apps/admin) |
| 2026-07-05 | S3 (B5 + cutover PWA) | **Cutover Supabase→D1 COMPLETO** (auth propia, catálogo Dexie, carrito en enteros, cobro por cola, banner) + pantallas Mostrador/Recepción/Inventario/Caja/Quiebres/Dashboard/Usuarios/Sucursales/Catálogo/Faltantes/Consolidado + impresión ESC/POS con fallback CSS 80mm + endpoints B5 (quiebres/dashboards/consolidado CSV/CRUD/mínimos/presentaciones Δ1). Supabase RETIRADO. **Gate: typecheck limpio · API 39/39 · PWA 5/5 · build SPA 88 módulos.** **E12 (deploy + GATE 4) BLOQUEADO** (clasificador de prod + navegador). | 236e7d8 (E10-E11 api) · f84bb0b (cutover PWA) · +test B5 (39/39) |
