{
  "name": "@huayruro/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Smoke e2e del POS contra wrangler dev sobre el seed sintético (E12, plan D1 §15.1/§18)",
  "scripts": {
    "test:e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:navegador": "playwright install chromium",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
